;;; org-mode-hacks --- Customizations to org-mode

;;; Commentary:

;;; Code:

(eval-when-compile
  (require 'org))

(setq org-log-done 'time)

(provide 'org-mode-hacks)
;;; org-mode-hacks.el ends here










